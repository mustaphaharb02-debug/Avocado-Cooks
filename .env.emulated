# Loaded only by `npm run dev:emulated` (vite --mode emulated).
# Points the site at the local Firebase emulators instead of the real
# project, so tests and experiments never touch live data.
VITE_USE_EMULATORS=1
